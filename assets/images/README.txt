Place your app logo image here as `logo.png`.
Recommended size: 240x240 (or height 120 used in splash).
Supported formats: png, jpg, webp.

When you add `logo.png`, run `flutter pub get` and restart the app if needed.